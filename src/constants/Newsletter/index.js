import { sliderData, heroImages } from "./NewsletterHero";
import { articleLinks, articleSectionData } from "./newsletterContent";

export { sliderData, heroImages, articleLinks, articleSectionData };
